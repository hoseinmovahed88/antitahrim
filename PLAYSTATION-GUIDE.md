# 🎮 راهنمای کامل باز کردن پلی‌استیشن از داخل ایران

<div dir="rtl">

## چرا پلی‌استیشن در ایران کار نمی‌کند؟

دو مانع جداگانه وجود دارد و هرکدام راه‌حل خودش را می‌خواهد:

| مانع | چه کسی اعمال می‌کند | نشانه |
|------|---------------------|-------|
| فیلترینگ و آلودگی DNS | اپراتور اینترنت ایران | صفحه لاگین اصلاً بالا نمی‌آید، خطای DNS |
| تحریم بر اساس آی‌پی | خود شرکت Sony و ناشران بازی | خطای `NW-31201-7`، `WS-116521-6`، `CE-107857-8` یا خطای Access Denied |

**نکته مهم:** خیلی‌ها فقط DNS را عوض می‌کنند و تعجب می‌کنند که هنوز کار نمی‌کند.
تغییر DNS فقط مانع اول را برمی‌دارد. برای مانع دوم ترافیک باید واقعاً از یک آی‌پی
غیرایرانی بیرون برود. این ابزار هر دو را با هم حل می‌کند.

---

## این ابزار چه کار می‌کند؟

یک دستگاه لینوکسی کوچک در خانه شما تبدیل به «دروازه پلی‌استیشن» می‌شود.
این دستگاه ترافیک کنسول را **تفکیک** می‌کند:

```
                         ┌─────────────────────────────┐
                         │  دروازه (رزبری‌پای / مینی‌پی‌سی) │
   ┌────────────┐        │                             │
   │ پلی‌استیشن  │───────▶│  دامنه Sony یا ناشر بازی؟   │
   └────────────┘        │        │            │       │
                         │       بله          نه       │
                         │        ▼            ▼       │
                         │   تونل Reality    مستقیم    │
                         └────────┼────────────┼───────┘
                                  │            │
                                  ▼            ▼
                         سرور شما در خارج    مودم خانه
                                  │            │
                                  ▼            ▼
                            PSN / فروشگاه   سرور بازی
```

**چرا تفکیک می‌کنیم و همه‌چیز را از تونل رد نمی‌کنیم؟**

اگر کل ترافیک از تونل رد شود سه اتفاق بد می‌افتد:

1. **NAT Type خراب می‌شود** و به Type 3 می‌رود. در این حالت خیلی از بازی‌های آنلاین
   لابی پیدا نمی‌کنند، صدای بازیکن‌ها قطع می‌شود و پرتاب از مسابقه اتفاق می‌افتد.
2. **پینگ بالا می‌رود.** اگر سرور بازی در امارات باشد و تونل شما در آلمان،
   پینگ شما به جای ۸۰ می‌شود ۲۵۰.
3. **پهنای باند سرور می‌سوزد.** دانلود یک بازی ۱۰۰ گیگابایتی از تونل، ترافیک سرور شما
   را تمام می‌کند.

این ابزار فقط ترافیکی را تونل می‌کند که واقعاً مسدود است. ترافیک UDP بازی
دست‌نخورده می‌ماند، پس **NAT Type شما همان چیزی می‌ماند که قبلاً بود.**

---

## 📦 چه چیزهایی لازم دارید؟

### ۱. یک سرور در خارج
سروری که با همین مخزن راه‌اندازی شده باشد:

```bash
./install-xray-reality-en.sh
```

در پایان نصب یک لینک `vless://...` می‌گیرید. آن را نگه دارید.

**انتخاب کشور سرور مهم است.** Sony رنج آی‌پی خیلی از دیتاسنترها را می‌بندد.
از بهترین به بدترین:

| کشور | وضعیت | توضیح |
|------|-------|-------|
| آلمان، هلند، فنلاند | 🟢 معمولاً خوب | پینگ خوب به سرورهای اروپایی بازی |
| ترکیه | 🟢 خوب | کمترین پینگ از ایران |
| امارات | 🟡 متغیر | پینگ عالی ولی بعضی رنج‌ها بسته است |
| آمریکا | 🟡 متغیر | پینگ بالا برای بازی آنلاین |
| روسیه | 🔴 پرریسک | بخش زیادی از رنج‌ها بسته است |

اگر بعد از نصب دیدید فروشگاه باز نمی‌شود ولی تونل سالم است، مشکل از رنج آی‌پی
سرور است نه از تنظیمات شما. سرور را جابه‌جا کنید.

### ۲. یک دستگاه لینوکسی در خانه
باید به همان مودمی وصل باشد که کنسول وصل است:

- رزبری‌پای ۳ یا ۴ (کم‌مصرف‌ترین گزینه، حدود ۳ وات)
- مینی‌پی‌سی یا ترین‌کلاینت دست‌دوم
- لپ‌تاپ قدیمی با اوبونتو
- ماشین مجازی روی کامپیوتری که همیشه روشن است

سیستم‌عامل: اوبونتو، دبیان یا Raspberry Pi OS. پردازنده x86 یا ARM هر دو کار می‌کند.

> ⚠️ **این دستگاه نباید سرور خارج باشد.** دروازه در خانه شما و کنار کنسول است.

---

## 🚀 نصب

روی دستگاه لینوکسی خانگی:

```bash
git clone https://github.com/hoseinmovahed88/antitahrim.git
cd antitahrim/playstation
sudo ./install-ps-gateway.sh
```

نصب از شما می‌پرسد:

1. **لینک `vless://`** سرور خارج. کافی است همان لینکی که موقع نصب سرور گرفتید
   را بچسبانید. اگر لینک ندارید Enter بزنید تا مقادیر را جدا جدا وارد کنید.
2. **آی‌پی کنسول.** می‌توانید فعلاً خالی بگذارید و بعداً ثبت کنید.

در پایان، آدرس دستگاه و پورت پروکسی را به شما نشان می‌دهد. یادداشت کنید.

---

## 🎮 تنظیم کنسول

### حالت پیشنهادی: پروکسی

این حالت پیش‌فرض است، ساده‌ترین است و **NAT Type را دست نمی‌زند.**

روی کنسول بروید به:

```
Settings → Network → Set Up Internet Connection
→ Use a LAN Cable  (یا Use Wi-Fi)
→ Custom
```

و این مقادیر را بزنید:

```
IP Address Settings ......... Automatic
DHCP Host Name .............. Do Not Specify
DNS Settings ................ Manual
    Primary DNS ............. 192.168.1.20      ← آی‌پی دستگاه دروازه
    Secondary DNS ........... 1.1.1.1
MTU Settings ................ Automatic
Proxy Server ................ Use
    Address ................. 192.168.1.20      ← همان آی‌پی دروازه
    Port .................... 8888
```

به جای `192.168.1.20` آی‌پی واقعی دستگاه خودتان را بگذارید. اگر یادتان نیست:

```bash
sudo ps-manage      # گزینه ۱
```

**بعد از ذخیره، `Test Internet Connection` بزنید.**

> اگر تست گفت `Internet Connection: Successful` ولی `PlayStation Network: Failed`،
> نگران نشوید. بعضی مدل‌ها در این تست پروکسی را نادیده می‌گیرند.
> ملاک واقعی این است که فروشگاه و لاگین باز شود.

### حالت دوم: دروازه شفاف

اگر بازی خاصی سرور خودش ایران را بسته و با حالت پروکسی باز نشد، این حالت را
روشن کنید. در این حالت همه ترافیک وب کنسول از دستگاه رد می‌شود، ولی همچنان
فقط دامنه‌های مسدود تونل می‌شوند و UDP بازی مستقیم می‌ماند.

```bash
sudo ps-mode console 192.168.1.50     # آی‌پی کنسول
sudo ps-mode gateway
```

سپس روی کنسول:

```
IP Address Settings ......... Manual
    IP Address .............. 192.168.1.50      ← همان که ثبت کردید
    Subnet Mask ............. 255.255.255.0
    Default Gateway ......... 192.168.1.20      ← دستگاه دروازه
    Primary DNS ............. 192.168.1.20
    Secondary DNS ........... 1.1.1.1
MTU Settings ................ Automatic
Proxy Server ................ Do Not Use
```

> ⚠️ در این حالت دستگاه دروازه باید همیشه روشن بماند، وگرنه کنسول کلاً
> اینترنت ندارد. برای برگشت: `sudo ps-mode proxy`

---

## 🛠 دستورهای مدیریت

| دستور | کار |
|-------|-----|
| `sudo ps-manage` | منوی کامل مدیریت |
| `sudo ps-check` | تست و عیب‌یابی کامل |
| `sudo ps-mode status` | نمایش وضعیت فعلی |
| `sudo ps-mode proxy` | رفتن به حالت پروکسی |
| `sudo ps-mode gateway` | رفتن به حالت دروازه شفاف |
| `sudo ps-mode console <ip>` | ثبت آی‌پی کنسول |
| `sudo ps-mode games on\|off` | تونل کردن دامنه ناشران بازی |
| `sudo ps-mode quic on\|off` | مسدودسازی QUIC کنسول |
| `journalctl -u ps-gateway -f` | لاگ زنده |

---

## 🔍 عیب‌یابی

**اول همیشه این را بزنید:**

```bash
sudo ps-check
```

این دستور تک‌تک سرویس‌های Sony را یک‌بار مستقیم و یک‌بار از تونل صدا می‌زند و
دقیقاً می‌گوید کدام مرحله خراب است.

### فروشگاه باز نمی‌شود ولی تونل سالم است

خروجی `ps-check` تونل را سالم نشان می‌دهد ولی ستون TUNNEL برای Sony قرمز است.
یعنی Sony رنج آی‌پی سرور شما را بسته.

راه‌حل به ترتیب:
1. سرور را به یک ارائه‌دهنده دیگر منتقل کنید (نه فقط کشور دیگر، ارائه‌دهنده دیگر).
2. ارائه‌دهنده‌ای انتخاب کنید که رنج خانگی یا Residential می‌دهد نه دیتاسنتر.
3. کشور حساب PSN را با کشور سرور هماهنگ کنید.

### بازی خاصی باز نمی‌شود

دامنه‌های ناشر آن بازی احتمالاً در لیست نیست. اضافه‌اش کنید:

```bash
sudo ps-manage      # گزینه ۵ → گزینه ۱
```

یا مستقیم:

```bash
echo "example-publisher.com" | sudo tee -a /etc/ps-gateway/domains/custom.list
sudo ps-genconfig && sudo systemctl restart ps-gateway
```

### NAT Type به ۳ رفت

اگر در حالت دروازه هستید:

```bash
sudo ps-mode proxy
```

و روی کنسول `IP Address Settings` را به `Automatic` و `Proxy Server` را به
`Use` برگردانید. حالت پروکسی هیچ‌وقت NAT Type را عوض نمی‌کند.

اگر در حالت پروکسی هم NAT Type خراب است، مشکل از مودم شماست نه از این ابزار.
روی مودم UPnP را روشن کنید یا پورت‌های PSN را فوروارد کنید.

### کنسول اصلاً اینترنت ندارد

معمولاً یعنی دستگاه دروازه خاموش شده و کنسول در حالت gateway مانده.
دستگاه را روشن کنید، یا موقتاً روی کنسول `IP Address Settings` را به
`Automatic` برگردانید تا مستقیم به مودم وصل شود.

### همه‌چیز سبز است ولی هنوز خطا می‌گیرم

بعضی کنسول‌ها تنظیمات شبکه را کش می‌کنند:

1. کنسول را کامل خاموش کنید (نه Rest Mode).
2. کابل برق را ۳۰ ثانیه بکشید.
3. روشن کنید و دوباره لاگین کنید.

### کنسول IPv6 می‌گیرد و دروازه دور زده می‌شود

اگر اپراتور شما IPv6 می‌دهد، PS5 ممکن است از آن مسیر مستقیم برود.
روی مودم IPv6 را خاموش کنید.

---

## ⚠️ این ابزار چه کاری نمی‌کند

صادقانه بگویم تا وقتتان تلف نشود:

- **مشکل پرداخت را حل نمی‌کند.** کارت بانکی ایرانی روی PSN کار نمی‌کند.
  برای خرید باید از گیفت‌کارت همان کشور حساب استفاده کنید.
- **حساب تحریم‌شده را برنمی‌گرداند.** اگر حساب شما از طرف Sony بسته شده،
  تغییر آی‌پی کاری نمی‌کند.
- **پینگ را بهتر نمی‌کند.** فقط جلوی بدتر شدنش را می‌گیرد.
- **بازی کرک‌شده اجرا نمی‌کند.** ربطی به این موضوع ندارد.
- **جای اینترنت پرسرعت را نمی‌گیرد.** اگر خط شما ضعیف است، دانلود بازی
  همچنان طول می‌کشد.

---

## 📁 فایل‌ها

| مسیر | کاربرد |
|------|--------|
| `/etc/ps-gateway/config.json` | کانفیگ Xray (دست‌ساز ویرایش نکنید) |
| `/etc/ps-gateway/server.env` | اطلاعات سرور خارج |
| `/etc/ps-gateway/settings.env` | تنظیمات دروازه |
| `/etc/ps-gateway/domains/psn.list` | دامنه‌های Sony |
| `/etc/ps-gateway/domains/games.list` | دامنه‌های ناشران بازی |
| `/etc/ps-gateway/domains/custom.list` | دامنه‌های دلخواه شما |

بعد از دست زدن به لیست دامنه‌ها حتماً بزنید:

```bash
sudo ps-genconfig && sudo systemctl restart ps-gateway
```

</div>

---

## English Summary

PlayStation consoles in Iran face two independent blocks: ISP-side filtering and
DNS poisoning, plus Sony's own IP-based sanctions. Changing DNS alone fixes only
the first one.

This toolkit turns a small Linux box on your home LAN into a split-routing
gateway. Sony and game-publisher domains go through your Xray Reality server
abroad; everything else, including all game UDP traffic, goes straight out. That
keeps your NAT type and ping unchanged.

**Install** on a Raspberry Pi, mini PC, or old laptop on the same LAN as the console:

```bash
git clone https://github.com/hoseinmovahed88/antitahrim.git
cd antitahrim/playstation
sudo ./install-ps-gateway.sh
```

Paste the `vless://` link from your server installation when prompted.

**Proxy mode** (default, recommended) needs no LAN changes. On the console set
DNS to the gateway IP and enable the HTTP proxy at the gateway IP on port 8888.

**Gateway mode** routes all console web traffic through the box for games whose
own servers block Iran. Run `sudo ps-mode console <ip>` then `sudo ps-mode gateway`,
and point the console's default gateway at the box.

Run `sudo ps-check` for a full diagnostic that tests every Sony endpoint both
directly and through the tunnel, so you can tell a broken tunnel apart from a
Sony-blocked server IP.
