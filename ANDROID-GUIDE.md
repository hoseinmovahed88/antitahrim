# 📱 آزاد — فیلترشکن اندروید بدون سرور و بدون کانفیگ

<div dir="rtl">

## دانلود

**[azad-arm64-v8a-debug.apk](https://github.com/hoseinmovahed88/antitahrim/releases/download/apk-latest/azad-arm64-v8a-debug.apk)** — حدود ۵۳ مگابایت

تقریباً همه گوشی‌های امروزی `arm64` هستند. اگر نصب نشد یا گوشی خیلی قدیمی
است، نسخه `armeabi-v7a` را از [صفحه انتشار](https://github.com/hoseinmovahed88/antitahrim/releases/tag/apk-latest)
بگیرید.

نسخه `debug` گزارش تشخیصی کامل نشان می‌دهد و برای عیب‌یابی مناسب‌تر است.
نسخه `release` چند مگابایت کوچک‌تر است.

---

## فرق این با بقیه پروژه چیست؟

بقیه ابزارهای این مخزن فرض می‌کنند شما یک سرور مجازی در خارج اجاره کرده‌اید و
یک لینک `vless://` دارید. این برنامه برای کسی است که هیچ‌کدام را ندارد.

| | ابزارهای دیگر این مخزن | آزاد |
|---|---|---|
| سرور مجازی | لازم است | لازم نیست |
| هزینه ماهانه | دارد | ندارد |
| کانفیگ دستی | باید وارد کنید | هیچ‌چیز وارد نمی‌کنید |
| سرعت و پایداری | بهتر، چون سرور مال خودتان است | متغیر |
| اعتماد | فقط به خودتان | به گردانندگان ناشناس سرورها |

اگر توان اجاره سرور را دارید، همان راه بهتر است. این برنامه برای وقتی است
که ندارید.

---

## دو روش اتصال

برنامه دو حامل دارد و در همان صفحه اول انتخابشان می‌کنید.

### ۱. سرورهای رایگان عمومی (پیش‌فرض)

گروه‌های مختلفی فهرست سرورهای رایگان را روی مخازن عمومی منتشر می‌کنند و
مرتب به‌روز می‌کنند. برنامه چند منبع را می‌گیرد، هزاران سرور را غربال
می‌کند و سریع‌ترین را برمی‌دارد.

```
  ۱. گرفتن فهرست‌ها      چند منبع عمومی، از پشت فیلترینگ
     │                    (خود آن مخازن هم معمولاً بسته‌اند)
     ▼
  ۲. غربال ارزان         بیست‌وچهار سرور هم‌زمان، فقط یک اتصال TCP.
     │                    هزاران تا به ده‌ها تا می‌رسد.
     ▼
  ۳. انتخاب              سریع‌ترینی که جواب داد
     │
     ▼
  ۴. اتصال               هسته Xray بالا می‌آید و پل tun2socks
                          بسته‌های گوشی را به آن می‌سپارد
```

سریع است و معمولاً از فیلترینگ رد می‌شود. سرور موفق ذخیره می‌شود تا دفعه
بعد اول همان امتحان شود.

**عیبش:** این سرورها را افراد ناشناس اداره می‌کنند و ترافیک شما از دستشان
رد می‌شود. برای عبور از فیلترینگ خوب است، برای کار حساس نه.

### ۲. تونل رایگان Cloudflare

از **WARP** استفاده می‌کند؛ سرویسی که Cloudflare رایگان و بدون حساب کاربری
به همه می‌دهد. برنامه روی خود گوشی یک جفت کلید می‌سازد، ثبت‌نام می‌کند، و
بین ده‌ها ترکیب آی‌پی و پورت می‌گردد تا یکی پیدا کند که در شبکه شما باز باشد.

به هیچ غریبه‌ای اعتماد نمی‌کند. **ولی روی بعضی شبکه‌های ایران کل پروتکلش
مسدود است** و هیچ ترکیبی جواب نمی‌دهد. برنامه این را تشخیص می‌دهد و صریح
می‌گوید که مشکل از اپراتور است نه از تنظیمات شما.

---

## DNS ایرانی و پنهان کردن SNI

### DNS ایرانی — اعمال شد

پیش از آنکه تونلی وجود داشته باشد، برنامه باید نام سرورها را پیدا کند.
برای این کار **فقط** از حل‌کننده‌های داخل ایران استفاده می‌کند: شکن،
الکترو، بگذر، رادار، ۴۰۳، شاتل و پیشگامان.

سه دلیل دارد:

1. DNS اپراتور برای دامنه‌های خارجی جواب جعلی می‌دهد.
2. پرس‌وجو به `8.8.8.8` یا `1.1.1.1` خودش نشانه استفاده از ابزار دور زدن است.
   حل‌کننده داخلی در ترافیک عادی گم می‌شود.
3. سریع‌تر است و ترافیکش داخلی حساب می‌شود.

پرس‌وجوی DNS با دست ساخته می‌شود و مستقیم روی UDP می‌رود، پس به هیچ
کتابخانه یا سرویس واسطی وابسته نیست.

**بعد از بالا آمدن تونل، DNS عوض می‌شود و می‌رود داخل تونل.** این عمدی
است: آنجا پرس‌وجوها رمزنگاری‌شده‌اند و اپراتور نمی‌بیندشان. اگر همان‌جا هم
DNS داخلی می‌گذاشتیم، تاریخچه کامل گشت‌وگذار شما را دستی به یک حل‌کننده
داخلی تحویل داده بودیم.

### SNI جعلی — عملی نیست، چیز بهتری جایش گذاشته شد

باید روراست باشم: **WireGuard اصلاً TLS ندارد.** تونل WARP یک پروتکل روی
UDP است و هیچ فیلد SNI در آن نیست که بشود جعل کرد.

جایی که TLS هست، درخواست‌های گرفتن فهرست و ثبت‌نام است. آنجا هم SNI جعلی
جواب نمی‌دهد: سرور وقتی SNI با هدر Host نخواند درخواست را رد می‌کند.

کاری که به جایش انجام شد و **واقعاً جواب می‌دهد**: اولین بسته دست‌دادن TLS
به چند قطعه TCP جدا شکسته می‌شود و بین قطعه‌ها چند میلی‌ثانیه مکث می‌افتد.
سامانه بازرسی که دنبال اسم دامنه در یک بسته می‌گردد، هیچ‌وقت اسم کامل را
در یک بسته نمی‌بیند.

این از SNI جعلی بهتر است، چون SNI را کاملاً پنهان می‌کند و سمت سرور هم
چیزی خراب نمی‌شود. گزینه‌اش در تنظیمات هست و پیش‌فرض روشن است.

**این روش از داخل ایران آزموده شده و کار می‌کند.** گزارش یک اجرای واقعی
نشان داد ۱۲۰۰ سرور از سه منبع عمومی گرفته شد، در حالی که خود آن مخازن از
ایران مسدودند.

---

## تنظیمات

| گزینه | پیش‌فرض | توضیح |
|-------|---------|-------|
| روش اتصال | سرورهای عمومی | حامل را انتخاب می‌کند |
| تکه‌تکه کردن دست‌دادن TLS | روشن | اسم دامنه را از دید بازرسی پنهان می‌کند |
| فقط DNS ایرانی | خاموش | اگر حل‌کننده‌های ایرانی جواب ندادند به DNS گوشی برنگردد |
| ترافیک داخلی مستقیم | خاموش | سایت‌های ایرانی از تونل رد نشوند |

### درباره «ترافیک داخلی مستقیم»

وقتی روشن باشد، فهرست رنج‌های ایران از کل فضای آدرس کم می‌شود و نتیجه به
عنوان مسیر تونل گذاشته می‌شود. سایت‌های ایرانی مستقیم باز می‌شوند: سریع‌تر،
با مصرف داخلی، و بدون اینکه سرور خارجی ببیند کجا می‌روید.

پیش‌فرض خاموش است چون فهرست حاصل حدود ۲۶۰۰ رنج می‌شود و روی گوشی‌های قدیمی
بالا آمدن تونل را کند می‌کند.

---

## حریم خصوصی

- کلیدها روی گوشی ساخته می‌شوند و هیچ‌وقت جایی فرستاده نمی‌شوند.
- برنامه هیچ آمار، هیچ گزارش خطا و هیچ شناسه‌ای به جایی نمی‌فرستد.
- گزارش تشخیصی فقط روی همین گوشی می‌ماند. دکمه کپی هست تا خودتان تصمیم
  بگیرید کجا بفرستیدش.

**توهم نداشته باشید:** در حالت سرورهای عمومی، ترافیک شما از سرور یک غریبه
رد می‌شود. در حالت Cloudflare، از Cloudflare. هر دو همان چیزی را می‌بینند
که هر ارائه‌دهنده VPN دیگری می‌بیند. اگر مدل تهدید شما در این حد است، این
ابزار مناسب شما نیست و باید سراغ Tor بروید.

---

## عیب‌یابی

**اول از همه:** دکمه «کپی گزارش کامل» را بزنید. گزارش هر مرحله را با زمان و
علت دقیق نشان می‌دهد و معمولاً خودش جواب را می‌گوید.

**«هیچ فهرست سروری دریافت نشد»**
اتصال اینترنت گوشی را بررسی کنید. اگر «فقط DNS ایرانی» روشن است خاموشش کنید.

**«هیچ‌کدام از سرورهای فهرست در دسترس نبودند»**
شبکه شما سخت‌گیرتر از حد معمول است. دوباره بزنید تا فهرست تازه گرفته شود.

**«تونل ساخته شد ولی هیچ ترافیکی عبور نکرد»**
سروری که انتخاب شد از کار افتاده. دوباره بزنید تا سرور دیگری امتحان شود.

**«اندروید اجازه ساخت تونل نداد»**
یک برنامه VPN دیگر تونل را در دست دارد. خاموشش کنید.

**می‌گوید متصل ولی اینترنت ندارم**
اگر آیکون کلید VPN در نوار وضعیت نیست، تونل واقعاً برقرار نشده.
«ترافیک داخلی مستقیم» را خاموش کنید و دوباره امتحان کنید.

**قبلاً کار می‌کرد، حالا نه**
«پاک کردن حساب و شروع دوباره» را بزنید تا سرور ذخیره‌شده فراموش شود.

---

## چه چیزی آزموده شده و چه چیزی نه

صادقانه، چون این مهم است.

**از داخل ایران روی گوشی واقعی آزموده شده:**

- گرفتن فهرست‌ها از مخازن عمومی که خودشان مسدودند. یک اجرای واقعی ۱۲۰۰
  سرور یکتا از سه منبع آورد.
- DNS ایرانی و تکه‌تکه کردن TLS، که همان مسیر بالا از آنها عبور می‌کند.
- غربال سرورها. ۵۴ سرور از ۹۶ تای تست‌شده جواب دادند، سریع‌ترین با ۴۰
  میلی‌ثانیه.

**در CI یا با تست واحد آزموده شده:**

- ثبت‌نام رایگان WARP، با یک ثبت‌نام واقعی در هر اجرای CI.
- ساخت و خواندن بسته DNS، و همچنین در برابر حل‌کننده‌های واقعی شکن و الکترو.
- خواندن لینک‌های `vless`، `vmess` و `trojan` روی داده واقعی همان فهرست‌ها.
- محاسبه مسیر «همه‌جا منهای ایران».
- اینکه کانفیگ تولیدشده هیچ‌وقت به فایل داده‌ای که همراه برنامه نیست تکیه نکند.

**هنوز آزموده نشده:**

- عبور واقعی ترافیک از تونل روی گوشی. تا این لحظه هر بار یک قدم جلوتر
  رفته‌ایم و آخرین مانع تازه برطرف شده.
- اینکه کدام سرور در شبکه شما پایدار می‌ماند. این ذاتاً به اپراتور و لحظه
  بستگی دارد.

---

## ساخت از روی کد

GitHub Actions با هر تغییر خودش می‌سازد و روی صفحه انتشار می‌گذارد.

اگر خودتان می‌خواهید بسازید، به یک قدم اضافه نیاز دارید: هسته Go باید با
gomobile به کتابخانه اندروید تبدیل شود.

```bash
cd tools/azadcore
gomobile bind -target=android/arm64,android/arm -androidapi 24 \
  -o ../../android/app/libs/azadcore.aar .

cd ../../android
./gradlew assembleDebug
```

</div>

---

## English Summary

**Azad** is an Android client that needs neither a VPS nor a config file. It
offers two transports and lets the user pick.

**Free public servers** is the default. Several groups publish lists of free
servers to public repositories; the app fetches them, probes thousands of
entries twenty-four at a time by TCP reachability, and stands up the Xray core
against the fastest survivor. A gVisor-based tun2socks bridge feeds the phone's
packets into that core. The trade-off is stated plainly in the UI: those servers
are run by strangers.

**Cloudflare's free tunnel** registers a WARP account on the device, keys and
all, and searches many endpoint IP and port combinations. It trusts no stranger,
but some Iranian networks block the WireGuard protocol outright, and the app
says so rather than pretending otherwise.

Name resolution before any tunnel exists goes through Iranian public resolvers
only, and the first TLS record of every bootstrap request is split across
several TCP segments so the server name never appears whole in one packet. Both
are confirmed working from Iran: a real run pulled 1200 servers from public
repositories that are themselves blocked there.

Download from the [releases page](https://github.com/hoseinmovahed88/antitahrim/releases/tag/apk-latest).
