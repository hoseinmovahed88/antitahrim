# 📱 آزاد — فیلترشکن اندروید بدون سرور و بدون کانفیگ

<div dir="rtl">

## فرق این با بقیه پروژه چیست؟

بقیه ابزارهای این مخزن فرض می‌کنند شما یک سرور مجازی در خارج اجاره کرده‌اید و
یک لینک `vless://` دارید. این برنامه برای کسی است که هیچ‌کدام را ندارد.

| | ابزارهای دیگر این مخزن | آزاد |
|---|---|---|
| سرور مجازی | لازم است | لازم نیست |
| هزینه ماهانه | دارد | ندارد |
| کانفیگ دستی | باید وارد کنید | هیچ‌چیز وارد نمی‌کنید |
| سرعت و پایداری | بهتر، چون سرور مال خودتان است | متغیر، چون شبکه عمومی است |
| کنترل | کامل | هیچ |

اگر توان اجاره سرور را دارید، همان راه بهتر است. این برنامه برای وقتی است
که ندارید.

---

## چطور کار می‌کند؟

برنامه از **Cloudflare WARP** استفاده می‌کند؛ سرویسی که Cloudflare رایگان و
بدون حساب کاربری به همه می‌دهد. برنامه رسمی خود Cloudflare هم دقیقاً همین کار
را می‌کند.

```
  ۱. تولید کلید            یک جفت کلید X25519 روی خود گوشی ساخته می‌شود.
     │                      کلید خصوصی هیچ‌وقت از گوشی بیرون نمی‌رود.
     ▼
  ۲. ثبت‌نام خودکار         کلید عمومی به Cloudflare فرستاده می‌شود و در جواب
     │                      یک آدرس داخلی و کلید همتا می‌گیریم.
     │                      نه ایمیل، نه رمز، نه پرداخت.
     ▼
  ۳. پیدا کردن نقطه باز     ده‌ها ترکیب آی‌پی و پورت امتحان می‌شود تا یکی
     │                      پیدا شود که واقعاً بسته رد و بدل کند.
     ▼
  ۴. اتصال                  تونل WireGuard بالا می‌آید.
```

مرحله سه مهم‌ترین بخش است. WARP خام در ایران معمولاً کار نمی‌کند چون پورت
پیش‌فرضش محدود شده. برنامه به جای اینکه تسلیم شود، ترکیب‌های دیگر را امتحان
می‌کند و اولین ترکیبی که جواب داد را ذخیره می‌کند تا دفعه بعد فوری وصل شوید.

**نکته‌ای که خیلی ابزارها اشتباه می‌کنند:** بالا آمدن تونل دلیل کار کردنش
نیست. WireGuard وقتی سرور اصلاً جواب نمی‌دهد هم «متصل» به نظر می‌رسد. این
برنامه بعد از هر اتصال یک پرس‌وجوی DNS از داخل تونل می‌فرستد؛ فقط اگر جواب
برگردد آن نقطه را قبول می‌کند.

---

## DNS ایرانی و پنهان کردن SNI

این دو مورد درخواست مستقیم بود، و اینجا صادقانه می‌گویم کجا اعمال شد و کجا
اصلاً معنا ندارد.

### DNS ایرانی — اعمال شد ✅

قبل از اینکه تونل بالا بیاید، برنامه باید نام `api.cloudflareclient.com` را
پیدا کند. برای این کار **فقط** از حل‌کننده‌های داخل ایران استفاده می‌کند:
شکن، الکترو، بگذر، رادار، ۴۰۳، شاتل و پیشگامان.

سه دلیل دارد:

1. DNS اپراتور برای دامنه‌های خارجی جواب جعلی می‌دهد.
2. پرس‌وجو به `8.8.8.8` یا `1.1.1.1` خودش نشانه استفاده از ابزار دور زدن است.
   حل‌کننده داخلی در ترافیک عادی گم می‌شود.
3. سریع‌تر است و ترافیکش داخلی حساب می‌شود.

پرس‌وجوی DNS با دست ساخته می‌شود و مستقیم روی UDP می‌رود، پس به هیچ کتابخانه
یا سرویس واسطی وابسته نیست.

**ولی بعد از بالا آمدن تونل، DNS عوض می‌شود و می‌رود به Cloudflare.** این
عمدی است: آن پرس‌وجوها داخل تونل رمزنگاری‌شده‌اند و اپراتور نمی‌بیندشان.
اگر همان‌جا هم DNS داخلی می‌گذاشتیم، تاریخچه کامل گشت‌وگذار شما را دستی به
یک حل‌کننده داخلی تحویل داده بودیم. یعنی دقیقاً برعکس چیزی که می‌خواهید.

### SNI جعلی — عملی نیست، چیز بهتری جایش گذاشته شد ⚠️

باید روراست باشم: **WireGuard اصلاً TLS ندارد.** تونل WARP یک پروتکل روی UDP
است و هیچ فیلد SNI در آن وجود ندارد که بشود جعل کرد. پس برای خود تونل، SNI
ایرانی بی‌معناست.

تنها جایی که TLS وجود دارد، همان یک درخواست ثبت‌نام اولیه است. آنجا هم SNI
جعلی جواب نمی‌دهد: Cloudflare از سال‌ها پیش وقتی SNI با هدر Host نخواند
درخواست را با خطا رد می‌کند. اگر SNI ایرانی می‌گذاشتیم، ثبت‌نام قطعاً شکست
می‌خورد.

کاری که به جایش انجام شد و **واقعاً جواب می‌دهد**: اولین بسته دست‌دادن TLS
به چند قطعه TCP جدا شکسته می‌شود و بین قطعه‌ها چند میلی‌ثانیه مکث می‌افتد.
سامانه بازرسی که دنبال اسم دامنه در یک بسته می‌گردد، هیچ‌وقت اسم کامل را در
یک بسته نمی‌بیند.

این از SNI جعلی بهتر است، چون SNI را کاملاً پنهان می‌کند نه اینکه با یک اسم
دیگر عوضش کند، و سمت سرور هیچ‌چیز خراب نمی‌شود.

گزینه‌اش در تنظیمات هست و پیش‌فرض روشن است.

---

## ساخت فایل APK

فعلاً APK آماده منتشر نشده. دو راه دارید.

### راه ۱: با GitHub Actions (بدون نصب چیزی)

فایل گردش‌کار آماده است ولی من اجازه نوشتن در پوشه `.github/workflows` را
نداشتم، پس خودتان باید یک بار جابه‌جایش کنید:

```bash
mkdir -p .github/workflows
cp android/ci/android.yml .github/workflows/android.yml
git add .github/workflows/android.yml
git commit -m "ci: add android build workflow"
git push
```

بعد از پوش، در تب **Actions** مخزن، هر بار که چیزی در پوشه `android/` عوض شود
برنامه ساخته می‌شود و فایل APK را می‌توانید از بخش Artifacts دانلود کنید.

این گردش‌کار سه کار می‌کند:
- تست‌های واحد را اجرا می‌کند
- APK دیباگ و ریلیز می‌سازد
- در یک کار جداگانه، ثبت‌نام واقعی WARP را امتحان می‌کند تا اگر Cloudflare
  قالب پاسخش را عوض کرد قبل از کاربرها بفهمیم

### راه ۲: با Android Studio

پوشه `android/` را در Android Studio باز کنید و `Run` بزنید. چیزی لازم نیست
تنظیم کنید.

یا از خط فرمان، اگر Android SDK دارید:

```bash
cd android
./gradlew assembleDebug
# خروجی: app/build/outputs/apk/debug/app-debug.apk
```

---

## تنظیمات برنامه

| گزینه | پیش‌فرض | توضیح |
|-------|---------|-------|
| تکه‌تکه کردن دست‌دادن TLS | روشن | اسم دامنه را از دید بازرسی بسته پنهان می‌کند |
| فقط DNS ایرانی | خاموش | اگر حل‌کننده‌های ایرانی جواب ندادند به DNS گوشی برنگردد. روشن کردنش امن‌تر است ولی ممکن است اصلاً وصل نشوید |
| ترافیک داخلی مستقیم | خاموش | سایت‌های ایرانی از تونل رد نشوند |

### درباره «ترافیک داخلی مستقیم»

وقتی روشن باشد، برنامه فهرست رنج‌های ایران را از کل فضای آدرس کم می‌کند و
نتیجه را به عنوان مسیر تونل می‌گذارد. یعنی سایت‌های ایرانی مستقیم باز می‌شوند:
سریع‌تر، با مصرف داخلی، و بدون اینکه Cloudflare ببیند کجا می‌روید.

پیش‌فرض خاموش است چون فهرست مسیر حاصل حدود ۲۶۰۰ رنج می‌شود و روی گوشی‌های
قدیمی بالا آمدن تونل را کند می‌کند. اگر گوشی‌تان جدید است روشنش کنید.

---

## حریم خصوصی

- کلید خصوصی روی گوشی ساخته می‌شود و هیچ‌وقت جایی فرستاده نمی‌شود.
- برنامه هیچ آمار، هیچ گزارش خطا و هیچ شناسه‌ای به جایی نمی‌فرستد.
- گزارش داخل برنامه فقط در حافظه است و با بستن برنامه پاک می‌شود.
- هیچ سروری از این پروژه در میان نیست. تنها طرف مقابل، خود Cloudflare است.

**ولی توهم نداشته باشید:** Cloudflare ترافیک شما را می‌بیند، همان‌طور که هر
ارائه‌دهنده VPN دیگری می‌بیند. اگر مدل تهدید شما در این حد است، این ابزار
مناسب شما نیست و باید سراغ Tor بروید.

---

## عیب‌یابی

**«هیچ نقطه اتصالی جواب نداد»**
شبکه شما همه پورت‌های UDP به Cloudflare را بسته. دوباره بزنید تا ترکیب‌های
دیگری امتحان شود. اگر با داده موبایل کار کرد ولی با وای‌فای نه، مشکل از
مودم یا شبکه است.

**وصل می‌شود ولی اینترنت ندارم**
«ترافیک داخلی مستقیم» را خاموش کنید و دوباره امتحان کنید.

**قبلاً کار می‌کرد، حالا نه**
دکمه «پاک کردن حساب و شروع دوباره» را بزنید. حساب تازه با کلید تازه ساخته
می‌شود و نقطه اتصال از نو پیدا می‌شود.

**اصلاً ثبت‌نام نمی‌شود**
یعنی به `api.cloudflareclient.com` نمی‌رسد. «فقط DNS ایرانی» را خاموش کنید
تا از DNS گوشی هم کمک بگیرد.

---

## چه چیزی تست شده و چه چیزی نه

صادقانه، چون این مهم است:

**تست شده:**
- ساخت و خواندن بسته DNS، با تست واحد و همچنین در برابر حل‌کننده‌های واقعی
  شکن و الکترو
- محاسبه مسیر «همه‌جا منهای ایران»، با تست واحد
- کامپایل شدن تمام منطق برنامه در برابر کتابخانه واقعی WireGuard
- مسیر سوکت خام و تکه‌تکه کردن TLS تا مرحله دست‌دادن

**تست نشده:**
- خود درخواست ثبت‌نام به Cloudflare. محیطی که این کد در آن نوشته شد اجازه
  خروج به آن میزبان را نمی‌داد. کار `warp-api` در گردش‌کار CI دقیقاً برای
  همین گذاشته شده؛ اولین اجرا این را روشن می‌کند.
- رفتار برنامه روی گوشی واقعی.
- صفحه رابط کاربری کامپایل نشده، چون کتابخانه‌های اندروید در آن محیط
  در دسترس نبودند.

</div>

---

## English Summary

**Azad** is an Android client that needs neither a VPS nor a config file. It
uses Cloudflare WARP, which anyone can register for free and anonymously. The
app generates an X25519 keypair on the device, registers the public key with
Cloudflare's own client API, and brings up a WireGuard tunnel. The private key
never leaves the phone.

Plain WARP is usually throttled in Iran, so the app probes many Cloudflare
endpoint IP and port combinations and keeps the first one that carries a real
DNS query end to end. A tunnel that comes up but moves no packets is treated as
a failure. The working endpoint is cached.

Name resolution before the tunnel exists goes through Iranian public resolvers
only, so a poisoned or conspicuous lookup does not break the bootstrap. Once the
tunnel is up, DNS moves inside it to Cloudflare rather than being handed to a
domestic resolver.

WireGuard carries no TLS and therefore no SNI, so a forged Iranian SNI is not
possible for the tunnel. For the one HTTPS call that does exist, the first TLS
record is split across several TCP segments, which hides the server name from
inspection entirely instead of replacing it.

Build it with Android Studio, or copy `android/ci/android.yml` into
`.github/workflows/` and let GitHub Actions produce the APK.
